#include<bits/stdc++.h>
#define outer0(b) for(int i=0;i<b;i++)
#define outer1(b) for(int i=1;i<=b;i++)
#define ll long long
#define nn "\n"
using namespace std;

int main(){
ios::sync_with_stdio(0);
cin.tie(0);
    ll n=0,q=0,x=0;
    cin>>n>>q;
    ll arr[n]{};
    outer0(n)cin>>arr[i];

    while(q--){
        ll cnt=0;
        cin>>x;
        for(int i=0;i<n;i++){
            
        }
        cout<<cnt<<nn;
    }
    
    return 0;
}