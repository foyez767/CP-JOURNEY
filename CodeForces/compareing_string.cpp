#include<bits/stdc++.h>
#define ll long long
#define nn  "\n"
using namespace std;

int main(){
ios::sync_with_stdio(0);
cin.tie(0);
  
    string s,str,st;
    cin>>s>>st;
    ll cnt=0;
    if(s.length()!=st.length()){
        cout<<"NO"<<nn;
        return 0;
    }
    else{
        for(int i=0;i<s.length();i++){
            
        }
    }
    (s==st) ? cout<<"YES"<<nn : cout<<"NO"<<nn;
    return 0;
}